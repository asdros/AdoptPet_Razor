@page
@model AdoptPet.Pages.WatchedItemsModel

@{
    ViewData["Title"] = "WatchedItems";
}

<h1>WatchedItems</h1>

<h4>WatchedItem</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="WatchedItem.AdId" class="control-label"></label>
                <select asp-for="WatchedItem.AdId" class ="form-control" asp-items="ViewBag.AdId"></select>
            </div>
            <div class="form-group">
                <label asp-for="WatchedItem.OwnerId" class="control-label"></label>
                <input asp-for="WatchedItem.OwnerId" class="form-control" />
                <span asp-validation-for="WatchedItem.OwnerId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
